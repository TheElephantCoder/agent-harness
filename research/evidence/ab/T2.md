Add a `--json` flag to the `summarize` command: when passed, print the stats as a single JSON object with keys `lines`, `words`, `chars`, `top` (top as an array of [word, count] pairs). The default text output must stay byte-identical. The existing test suite (`npm test`) must keep passing.

Work in the current directory. When done, summarize what you changed.
