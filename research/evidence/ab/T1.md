Write ANSWER.md explaining what the `summarize` command outputs for the exact input `hi hi hello` (run it yourself to see), and which files implement parsing, counting, and output. Name both implementation files and quote the exact full output in the answer.

Work in the current directory. When done, summarize what you changed.
