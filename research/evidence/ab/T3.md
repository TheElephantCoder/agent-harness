The `summarize` command miscounts characters for non-ASCII input: piping `👍👍 hi` into it reports `chars: 7`, but the visible text has 5 characters. Fix the counting so user-perceived characters are counted. The existing test suite (`npm test`) must keep passing.

Work in the current directory. When done, summarize what you changed.
