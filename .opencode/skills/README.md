# .opencode skills shim

This directory is populated by `harness init --harness opencode` from `skills/*/SKILL.md`.

Do not edit manually: edit `skills/` and run `harness doctor --fix`.

See `opencode.json` for hook wiring.
